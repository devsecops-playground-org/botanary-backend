# Copy to .env and fill in. Never commit the result.
APP_ENV=development
PORT=8080

# Required in production; the service refuses to boot without them.
DATABASE_URL=postgres://botanary:botanary@postgres:5432/botanary?sslmode=disable
JWT_SECRET=
